[package]
name = "umbral-socket"
version = "0.3.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/alan-venv/umbral-socket"
description = "Lightweight IPC client and server over Unix sockets"

[dependencies]
mio = { version = "1.2.0", features = ["net", "os-poll"] }
